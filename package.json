{
  "name": "my-app",
  "version": "0.1.0",
  "main": "server.js",
  "private": true,
  "dependencies": {
    "argon2": "^0.26.1",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.6"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev &&  npm install && chmod + node_modules/.bin/react-scripts &&  npm run build"
  },
  "engines": {
    "node": "12 || 10"
  }
}
